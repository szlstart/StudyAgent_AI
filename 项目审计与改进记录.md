# StudyAgent AI 项目审计与改进记录

> 审计日期：2026-09-03  
> 结论：核心学习闭环已经可用，适合作为本机单用户进程的学习助手；本轮重点补齐了真实使用中容易被忽略的数据一致性、内容安全、错误降级和交互竞争问题。项目仍有若干明确的产品边界，不能把“能运行”等同于“可直接公开部署”。

## 一、项目当前优点

### 1. 学习闭环完整

项目已经串起“登录档案 → 按年级显示科目 → 上传作业 → 视觉识别和批改 → 对应教材 RAG → 错题归档 → 学情画像 → 后续复习和问答”的完整流程。作业结果、错题、历史记录和本地结构化画像会在响应返回前保存，学生批改完成后立即刷新即可看到最新数据。

### 2. 教材边界清楚

- 只支持一年级至九年级，不包含高中课程。
- 科目严格由年级决定。
- 教材由“年级 + 学期 + 科目”唯一确定。
- 旧年级的教材链接不能在修改档案后静默打开新教材。
- 70 本教材均已有清单，并在 Qdrant 中完成向量索引。

### 3. 图片作业处理符合实际场景

支持 JPG、PNG、WEBP、GIF、HEIC 和 HEIF；视觉模型会同时理解文字、手写内容、公式、图形和图表。HEIC/HEIF 会转换成模型稳定支持的格式，同时在学生私有目录保留原文件。多页作业会保留页码顺序，错题解析可以回看对应原图。

### 4. 用户资料隔离较完整

SQLite 保存姓名、年级、学期和登录会话；每个用户的作业图片、错题、历史、聊天和本地画像均存放在独立目录。EverOS 也使用用户 UUID 隔离长期记忆，而不是共用一个全局身份。

### 5. 界面适合当前产品定位

界面已经统一为浅色主题，桌面端有完整左侧导航，移动端有底部导航；前进、后退、空状态、加载状态和操作反馈均已存在。主控台、作业批改、错题本、题目询问、知识库、历史记录和“我的”七个页面均可正常进入。

## 二、本轮发现并已修复的问题

### P0：AI 内容可能通过 Markdown 注入危险 HTML

问题：错题解析、问答消息和历史消息使用 `x-html` 展示模型生成的 Markdown。Markdown 渲染器默认允许原始 HTML，恶意或异常内容可能插入脚本、事件属性、Alpine 指令、危险链接或表单元素。

修复：在 Markdown 渲染后增加 DOM 净化，移除脚本、内嵌页面、表单、图片、事件属性、Alpine 指令、危险 URL 和危险内联样式；普通 Markdown、代码、链接和 KaTeX 公式仍可展示。纯文本降级路径也先做 HTML 转义。

### P0：并发请求可能覆盖记忆或错题

问题：不同路由会为同一用户创建不同的 `MemoryService` 和 `WrongBookService` 实例。原先每个实例各有一把锁，不能阻止两个实例同时执行“读取 → 修改 → 写回”，可能丢失其中一次更新。聊天会话的读取和写入之间也没有覆盖整个事务。

修复：同一路径的服务实例共享文件锁；聊天的读取、修改和保存放进同一个临界区；记忆、会话、错题、题库、作业图片和作业历史均采用临时文件完成后原子替换，避免读到半写入文件。

### P0：第一次模型答案被永久当成题库权威答案

问题：客观题第一次由模型生成的标准答案会直接进入缓存，之后可绕过模型判题。如果第一次答案错误，错误会被长期放大。

最终修复：题库默认只采集、绝不参与正式判分；只有管理员显式开启实验开关后，经过至少两次一致确认的选择题才可能命中。填空题不再做字符串缓存判分。题目标准化保留数学运算符，避免 `2+3` 与 `2-3` 碰撞。详见《严格安全与Agent质量审计报告.md》SB-02、SB-03。

### P1：答题请求可能意外触发整本教材向量化

问题：教材未索引或索引失效时，一次普通答题可能自动开始整本 PDF 的 OCR/Embedding/Qdrant 写入，导致请求异常缓慢，并产生不可预期的 API 用量。

修复：批改和问答只检索已经就绪的索引；未就绪时快速降级为无 RAG，并明确保留 `used_rag=false`。教材重建只允许通过知识库页面或维护脚本显式触发。

### P1：Qdrant 客户端连接未及时关闭

问题：每次状态检查、索引或检索都会创建客户端，但原实现没有关闭连接。长时间运行后可能积累连接资源。

修复：同步和异步 Qdrant 客户端均使用 `finally` 关闭；教材状态检查改为在线程中并行执行，Qdrant 异常时不会长时间阻塞 FastAPI 事件循环。

### P1：内部异常直接显示给学生

问题：部分 500 响应把原始异常文本返回给页面，可能暴露内部路径、上游接口信息或配置细节，也不利于学生理解。

修复：服务日志继续记录完整堆栈，页面只收到简洁、可操作的中文提示。教材索引失败也不再返回内部错误原文。

### P1：本机服务仍缺少 Host 与浏览器响应防护

修复：只接受 `localhost`、`127.0.0.1` 和测试环境主机名；未知 Host 返回 400。继续把 `127.0.0.1` 统一重定向到 `localhost`。新增禁止 iframe 嵌入、禁止 MIME 猜测、无来源信息、权限限制和 CSP 等响应头。

### P1：快速修改年级或偏好可能出现旧响应覆盖新选择

问题：连续快速切换下拉框时，较早请求可能较晚返回，使界面最终显示旧值。

修复：年级/学期和个人偏好分别增加请求序号，只允许最后一次请求更新界面与提示。

### P2：输入验证和小范围显示缺口

- MBTI 现在只接受空值或标准 16 型，且限制长度。
- 个性化问候的科目映射补齐“地理”。
- 题库统计接口会拒绝不属于当前年级的科目。
- 新增 `pytest.ini`，在项目目录直接运行 Conda 环境中的 `pytest` 即可找到 `src`，不再依赖手动设置 `PYTHONPATH`。

## 三、仍然存在的限制

### 1. 登录不是安全账号系统

这是当前产品明确接受的设计：姓名唯一、无密码、首次登录自动建档。知道姓名的人就能进入对应档案，因此只能在可信任的个人 Mac 上使用，不能直接部署为公网或多人不可信环境。

### 2. 文件数据层只适合单个 StudyAgent AI 进程

本轮的共享锁解决了同一 Python 进程内的并发问题，原子替换解决了半写入文件问题；但 JSON 文件没有跨进程事务锁。不要同时启动两个 StudyAgent AI 后端。若未来需要家庭多设备或学校部署，应把错题、历史和画像整体迁移到 PostgreSQL。

### 3. 前端仍依赖公共 CDN

Tailwind、Alpine、Marked、KaTeX 和字体当前从公共 CDN 加载。优点是项目简单、无需 Node 构建；缺点是首次打开需要联网，Tailwind 浏览器版会产生开发版警告，也无法做到完全离线。后续发布正式桌面版时，应固定版本并把这些资源构建或下载到本地。

### 4. AI 与 Embedding 是外部依赖

模型接口故障、余额不足、限流或网络异常都会让批改和问答降级或失败。Qdrant 中已有的向量不会丢失，但新查询仍需要调用 embedding 接口生成查询向量。

### 5. 后台长期记忆是“尽力写入”

核心批改结果、本地历史和结构化画像同步保存；题库学习与 EverOS 写入属于后台增强任务。若后端在响应后立刻被强制结束，最后一个增强任务可能来不及完成，但不会影响已经返回的批改和本地记录。

### 6. 教材质量仍受 PDF 文字层影响

已有 70 本教材已索引，但扫描页、复杂图表、竖排文本和公式的检索效果取决于 PDF 文字层与 OCR 质量。视觉模型能理解学生上传的作业图片，不代表教材中的每张复杂插图都已经被向量文本完整表达。

## 四、验证证据

- Python 全量测试：28 项通过。
- 新增并发回归：两个 `MemoryService` 实例并发写同一会话，20 轮记录无丢失。
- 新增题库回归：默认不允许缓存判分；实验开关下首次答案不命中、第二次一致确认后才允许选择题命中；数学运算符不会产生缓存键碰撞。
- 新增安全回归：非本机 Host 被拒绝；安全响应头存在；非法 MBTI 被拒绝。
- Conda 依赖检查：无损坏或缺失依赖。
- Docker：EverOS 与 Qdrant 均为 healthy，且只映射到 `127.0.0.1`。
- 教材：70/70 份索引清单有效，70/70 份通过 Qdrant 精确存在性检查，共 13,643 个向量点。
- 实际 RAG：九年级下册化学查询返回 3 个片段，全部属于 `grade_09_lower_chemistry`。
- 真实浏览器：登录后七个主要页面均可进入，没有 `Failed to fetch` 或“加载档案失败”；控制台无运行错误，仅有 Tailwind CDN 的正式环境提醒。
- 地址边界：`127.0.0.1:8001` 返回 308 到 `localhost:8001`；伪造外部 Host 返回 400。

## 五、维护建议优先级

1. 立即：曾经在聊天、截图或其他位置公开过的 API 密钥应在服务商后台轮换，项目中只保留新密钥；不要在文档或提交中保存真实值。
2. 日常：只启动一个 `./run.sh` 进程；定期备份 SQLite、`data/users`、EverOS 卷和 Qdrant 卷。
3. 发布前：本地化前端 CDN 资源，加入真正的前端构建与浏览器端自动化测试。
4. 扩展到多人设备前：加入密码或家长 PIN，把 JSON 数据迁移到事务数据库，再重新设计网络访问边界。
