# StudyAgent AI 严格安全、用户体验与 Agent 质量审计报告

审计日期：2026-09-04
审计对象：当前工作区、实际运行服务、浏览器页面、Qdrant、EverOS、GPT-5.4 与千问 Embedding 链路
结论口径：只把本轮亲自验证的行为写为“已验证”；设计限制和未做的事情单独列出。

---

## 一、执行结论

本轮不是只做界面检查，而是沿着“浏览器输入 → FastAPI → Agent → 模型 → RAG → 本地数据 → EverOS”的完整信任链审查。

审计前存在会直接影响批改正确性或数据安全的严重问题：

1. 模型批量返回漏题或重排时，后一道题的判分可能错套到前一道题。
2. 题库键删除了数学运算符，`2+3` 与 `2-3` 可能被当成同一道题；历史模型答案还可能绕过正式模型成为“标准答案”。
3. 错题 JSON 中保存的图片路径被直接读取；数据一旦被篡改，可能把当前用户目录之外的本机文件发送给外部视觉模型。
4. 画像、会话或错题 JSON 损坏时会被当成空数据；下一次写入可能覆盖原文件，造成不可恢复的数据丢失。
5. 多页 OCR 某一页失败会被静默跳过，用户仍可能收到一份看起来完整、实则缺页的批改结果。
6. 图片只按扩展名或 MIME 声明判断，伪造文件和超大像素图片缺少真实解码校验。
7. RAG 在向量库或 Embedding 故障时会拖垮答疑；同时没有相关性门槛，明显无关内容也可能被作为教材依据。
8. 本地“薄弱知识点”和错误类型只增不减，学生掌握、删除或申诉成功后仍会被旧画像长期贴标签。
9. 题目、教材、历史记忆和图片内文字可以进入 Agent 提示词，但原有提示词没有统一定义“这些内容只是数据，不是指令”。
10. 性别、MBTI 会进入模型和长期记忆，既不是可靠的学习能力证据，也属于不必要的未成年人画像信息。

上述问题均已在本轮修复或隔离。当前全量自动化测试为 **28 项通过**；真实浏览器七个主要页面可访问；70/70 本教材索引有效；Qdrant 与 EverOS 健康；九年级化学真实模型问答成功使用正确教材 RAG。

但本项目仍不能被描述成“适合公网或学校多人环境的安全产品”。姓名登录没有密码是产品明确接受的约束，公共 CDN、单进程 JSON 数据层、第三方模型数据处理和 AI 判分的不确定性仍属于剩余风险，详见第七部分。

---

## 二、审计范围与方法

### 2.1 审查范围

- 登录、Cookie、Host、CORS、API 鉴权与本机访问边界；
- 作业图片上传、文件格式、大小、像素、页数、文件路径；
- OCRAgent、GradeAgent、KnowPointAgent、ExamTagAgent、ExplainAgent、ChatCompressor；
- 题库缓存、本地画像、错题本、历史记录、会话图片；
- 教材 PDF、Qdrant 向量索引、检索相关性与降级行为；
- EverOS 长期记忆的用户隔离、错误结论、敏感画像与故障影响；
- 前端主要页面、导航、错误反馈、Markdown 渲染和浏览器控制台；
- Python 依赖完整性和公开漏洞数据库检查。

### 2.2 验证方法

- 静态追踪所有实际注册路由，而不是把仓库里存在的旧文件误认为线上功能；
- 为高风险缺陷增加定向测试，再执行全量测试；
- 用真实浏览器逐页点击主控台、作业批改、错题本、题目询问、知识库、历史记录和“我的”；
- 实测页面前进/后退；
- 使用真实登录态请求健康、模型、教材和已移除旧路由；
- 对 Qdrant 执行 70 本教材精确存在性检查；
- 分别用课程内查询和明显越级查询校准 RAG 分数门槛；
- 使用真实 GPT-5.4 完成一次九年级下册化学教材问答；
- 执行 `pip check` 和 `pip-audit -r requirements.txt`。

---

## 三、发现与修复清单


| 编号  | 严重度 | 问题                                                 | 处理结果                                                                              |
| ------- | -------- | ------------------------------------------------------ | --------------------------------------------------------------------------------------- |
| SB-01 | 严重   | 批量判分依赖数组位置，漏题会造成错题错配             | 已修复：稳定`item_id`，唯一题号兼容，安全位置回退，缺项标记 `skip`                    |
| SB-02 | 严重   | 题库标准化删除`+ - =` 等符号，数学题可碰撞           | 已修复：NFKC + 空白归一化，保留运算符，缓存版本升级为 2                               |
| SB-03 | 严重   | 历史模型答案可直接绕过 GPT-5.4 正式判分              | 已修复：默认关闭题库直接判分；填空题永不字符串缓存判分                                |
| SB-04 | 高     | 持久化图片路径可指向用户目录外文件                   | 已修复：解析和复审前强制解析路径并验证位于当前用户目录                                |
| SB-05 | 高     | 损坏 JSON 被当作空数据，后续写入可覆盖原文件         | 已修复：画像、会话、错题、题库全部失败关闭并保留损坏原件                              |
| SB-06 | 高     | 多页 OCR 失败页被静默忽略                            | 已修复：返回具体失败页，整份批改停止，不生成伪完整结果                                |
| SB-07 | 高     | OCR 并发、页数、题数和文本长度缺少边界               | 已修复：12 页、每页 80 题、整批 300 题、OCR 并发 3、字段截断                          |
| SB-08 | 高     | 假图片、损坏图片、超大像素图片可进入模型链路         | 已修复：Pillow 真实解码、格式识别、40MP 像素上限、20MB 流式上限                       |
| SB-09 | 高     | RAG 故障导致答疑/批改整体 500                        | 已修复：Embedding/Qdrant 异常自动降级，明确`used_rag=false`                           |
| SB-10 | 高     | 无相关性阈值，越级或无关片段被包装成教材依据         | 已修复：默认相似度阈值 0.35，可由`RAG_MIN_SCORE` 调整                                 |
| SB-11 | 高     | 薄弱点与错误类型只增不减，形成过时学生标签           | 已修复：以当前未掌握错题本为事实源重算，掌握/删除/申诉后同步缩减                      |
| SB-12 | 高     | 题目、教材、记忆中的提示注入可改变 Agent 行为        | 已缓解：所有活跃 Agent 增加不可信数据边界和禁止泄露规则                               |
| SB-13 | 高     | 模型返回的得分可为负数或超过题目满分                 | 已修复：正确/错误按确定规则计分，部分正确得分限制在 0 到满分                          |
| SB-14 | 中     | 相同秒、相同题号的错题 ID 可能碰撞并丢记录           | 已修复：错题批次加入随机 UUID，题号安全截断                                           |
| SB-15 | 中     | 重复点击重建教材可并发删除/写入同一索引              | 已修复：按教材加锁、任务去重，重复请求返回“已在运行”                                |
| SB-16 | 中     | `is_indexed` 在异步检索中同步阻塞事件循环            | 已修复：移到线程执行，查询`top_k` 也限制为 1–10                                      |
| SB-17 | 中     | 性别、MBTI 被送入模型和 EverOS 并影响画像            | 已修复：仅本机保存用于界面；不进入 Agent 决策、不写入新 EverOS 记忆，旧记录检索时过滤 |
| SB-18 | 中     | EverOS 旧错误记录在申诉或掌握后仍可能被召回          | 已缓解：写入显式“学情更正”；正式判分完全不读取 EverOS                               |
| SB-19 | 中     | 未使用的全局`/settings` 路由扩大维护和跨用户状态风险 | 已修复：从应用注册表移除，接口实测返回 404                                            |
| SB-20 | 中     | SQLite 和分用户资料默认可被其他本机账户读取          | 已修复：数据库/文件改为 0600，分用户目录改为 0700，并迁移现有数据权限                 |
| SB-21 | 中     | 部分前端操作只显示`HTTP 500`，丢失可操作错误原因     | 已修复：统一提取后端`detail`，无 JSON 时才显示状态码兜底                              |
| SB-22 | 中     | 前端可连续加入超过后端上限的图片，最后才失败         | 已修复：选择第 13 页时立即阻止并说明需分批批改                                        |
| SB-23 | 低     | ChatCompressor 把产品写死成“初中生”                | 已修复：明确一年级至九年级；摘要输入同样标记为不可信资料                              |
| SB-24 | 低     | ExplainAgent 缺少地理中文名称                        | 已修复：补齐地理映射                                                                  |

---

## 四、Agent 架构合理性审查

### 4.1 当前真正在线的 Agent

当前注册路由实际使用：

1. **OCRAgent**：视觉理解原图，再把描述结构化为题目；
2. **GradeAgent**：结合原图、OCR 结果和当前教材片段逐题判分；
3. **KnowPointAgent**：标注知识点、难度与历史薄弱点命中；
4. **ExamTagAgent**：归纳整份作业的结构标签和薄弱点；
5. **ExplainAgent**：教材检索、错题解析和主动答疑；
6. **ChatCompressor**：压缩较长对话，形成可检索摘要。

仓库中 `question`、`quiz`、旧 `knowledge` 路由、旧 `settings` 路由和 `OCRGradeAgent` 不属于当前在线链路。`settings` 已取消注册，`OCRGradeAgent` 也不再从 homework 包导出。其他旧模块仍留在源码中，只作为未注册遗留代码，不能据其存在宣传功能。

### 4.2 合理之处

- OCR 与判分分离是合理的：OCR 负责“看到了什么”，Grade 负责“是否正确”，便于分别失败和测试。
- Grade 仍收到原始图片，而不是只信 OCR 文字；图形、圈画、位置关系和手写步骤不会被强迫降级成纯 OCR。
- 教材检索按用户登录时的年级、学期、科目生成唯一 `textbook_id`，不会在请求里让模型自行猜教材。
- KnowPoint 和 ExamTag 属于增强层；失败时不应抹掉已经完成的正式判分。
- 本地结构化画像负责可计算的当前事实，EverOS 负责跨会话自然语言线索，两者职责现在更清楚。

### 4.3 原有严重不合理之处及调整

**判分链路曾信任模型输出顺序。** 这不符合“判分必须逐题可追溯”的要求。现在 Grade 和 KnowPoint 都使用稳定 ID；响应漏项只影响该项，不会污染相邻题。

**题库缓存曾充当权威答案库。** 它实际只是模型历史输出，没有人工审核或出版社答案来源，不应绕过 GPT-5.4 和原图。现在默认只采集，不参与正式判分。

**长期记忆曾进入正式评分。** 自然语言记忆可能过时、被提示注入或包含已撤销判决，不应改变答案和评分。现在正式判分只使用当前本地结构化画像，且画像只能帮助识别错误类型，不能降低标准；EverOS 只用于讲解个性化。

**MBTI/性别曾被当成教学依据。** 对未成年人进行这种能力推断没有可靠依据。现在它们不进入 Agent 或新长期记忆，也不会影响判分、RAG 和教材选择。

**RAG 曾被当成绝对真理。** 向量检索只是近似召回，不能保证 top-k 都相关。现在增加分数门槛；有片段时以教材为主要依据，允许符合当前年级的必要推理，但不得伪造教材出处；无片段时明确降级。

### 4.4 仍需正确理解的边界

- 这些组件叫“Agent”，但它们不是能自由操作系统的自治智能体，本质是有边界的模型调用编排器。这样的限制对学生产品是优点。
- Prompt 防注入是缓解措施，不是数学意义上的安全证明。真正的安全边界仍由路由鉴权、文件路径校验、固定模型配置和不向 Agent 提供系统工具来保证。
- GPT-5.4 视觉能力适合综合理解照片中的文字、公式、图形和圈画；OCR 是结构提取步骤，不应替代视觉复核。当前设计遵循这一点。
- KnowPoint 与 ExamTag 的输出是模型分析，不是教材权威元数据。界面可以展示，但不应把它们作为升学评价或教师最终结论。

---

## 五、数据与隐私审查

### 5.1 已形成的安全边界

- Web、Qdrant、EverOS 均只绑定本机回环地址；
- 浏览器统一使用 `localhost`，`127.0.0.1` 自动 308 跳转；
- 每个用户有 UUID 独立目录和 EverOS user ID；
- 作业图片没有公开静态挂载；
- 错题图片回读必须位于当前用户目录；
- `.env` 权限为 0600，并被 `.gitignore` 忽略；
- SQLite、画像、错题、历史和图片现在使用私有权限；
- Markdown 经过 DOM 净化，脚本、事件属性、Alpine 指令、危险链接和表单元素会被移除；
- LLM 日志只记录长度和阶段，不记录完整题目、学生答案或模型全文。

### 5.2 长期记忆一致性

本地结构化画像现在区分：

- 历史成绩记录：保留当时发生过的事实；
- 当前薄弱点与错误分布：根据尚未掌握的错题动态重算；
- 申诉成功：修正对应历史统计、删除错题、重算当前画像；
- EverOS：追加一条“旧判断作废”的更正记录。

这比“所有内容永久只增不减”可靠，但 EverOS 1.2.3 当前集成仍是追加式事件，不等于彻底物理删除旧记忆。检索冲突时已要求新更正优先，正式判分也不再使用 EverOS。

---

## 六、验证结果

### 6.1 自动化与静态检查

- `python -m compileall -q src`：通过；
- `python -m pytest -q`：**28 passed**；
- `python -m pip check`：No broken requirements found；
- `python -m pip_audit -r requirements.txt`：No known vulnerabilities found；
- 新增测试覆盖：判分重排、漏项不串题、重复题号知识点对齐、数学符号缓存键、默认禁用缓存判分、损坏文件失败关闭、用户路径逃逸、掌握后画像清除、OCR 失败页、伪图片拒绝。

### 6.2 数据与基础设施

- Docker `everos`：healthy，仅 `127.0.0.1:1995`；
- Docker `qdrant`：healthy，仅 `127.0.0.1:6333`；
- 教材 PDF：70 本；
- 索引清单：70 份；
- Qdrant 精确检查：70/70 本 ready；
- Qdrant 点数：13,643；
- Conda：所有验证均使用 `/opt/miniconda3/envs/studybuddy/bin/python`，未使用 `.venv`。

### 6.3 RAG 与真实模型

- 九年级下册化学“铁生锈条件”查询：4 个相关片段，分数约 0.44–0.53；
- 明显越级的“量子引力黑洞霍金辐射”查询：阈值过滤后 0 个片段；
- 真实 GPT-5.4 答疑：HTTP 200，`used_rag=true`，返回 3 个教材展示片段，回答包含水与氧气/空气的关键条件。

### 6.4 真实浏览器

- 主控台、作业批改、错题本、题目询问、知识库、历史记录、“我的”均可进入；
- 九年级下册只显示语文、数学、英语、物理、历史、化学，符合课程规则；
- 知识库显示并打开当前九年级下册教材，不复用旧年级 URL；
- 前进按钮和后退按钮实测可回到正确页面；
- 页面无 `Failed to fetch`、无重复“加载档案失败”；
- 控制台无运行错误；仅剩 Tailwind 浏览器 CDN 的正式环境警告。

---

## 七、剩余风险与明确限制

### R-01：姓名登录没有身份保护（高，产品已明确接受）

知道姓名的人可以选择该姓名登录并进入对应档案，也可以修改年级和学期。这是“姓名唯一、无密码、首次登录自动建档”的必然结果，不可能同时声称具备账号安全。

因此当前项目只能用于可信任的个人 Mac。若要给学校、家庭多设备或公网使用，必须增加 PIN/密码、家长或教师管理、会话撤销、审计日志和数据删除能力。

### R-02：AI 判分不是考试级最终裁决（高）

即使有视觉复核、教材 RAG、稳定 ID 和申诉流程，模型仍可能看错手写、误解题意或给出错误答案。高风险考试、作文、证明题和模糊照片必须保留人工复核。当前“重新评分”和“标记为正确”能纠错，但不能证明第一次结果绝对正确。

### R-03：前端依赖公共 CDN（中）

Tailwind、Alpine、Marked、KaTeX 和字体仍从公共 CDN 加载。断网或 CDN 故障会造成样式或交互不可用；运行时 Tailwind 还要求 CSP 保留 `unsafe-eval`，浏览器会显示开发版警告。

正式交付前应增加一次前端构建，把 Tailwind 编译为静态 CSS，并把 Alpine、Marked、KaTeX 和字体固定版本后放到 `web/vendor/`，随后移除 CSP 的 `unsafe-eval` 和外部域名。

### R-04：JSON 数据层只支持单个后端进程（中）

共享异步锁和原子替换解决了单进程并发及半写入问题，但不能提供跨进程事务。不能同时启动两个 StudyAgent AI 后端。未来多人部署应把错题、历史、画像和会话迁移到 SQLite 完整事务或 PostgreSQL。

### R-05：EverOS 为追加式、尽力写入（中）

后台进程刚返回结果就被强制关闭时，最后一批长期记忆可能未写完。申诉和掌握现在会追加更正，但旧记忆仍可能物理存在。需要彻底“被遗忘”时，应实现 EverOS 用户级/记忆级删除 API 和界面。

### R-06：模型与 Embedding 会处理学习内容（中）

作业图片、题目、答案、教材查询和必要的学习上下文会发送给配置的模型或 Embedding 服务。不得上传身份证、住址、联系方式、医疗信息等无关敏感内容。当前系统也不应被宣传为完全离线。

### R-07：遗留源码仍较多（低到中）

未注册的旧 `question`、`quiz`、`knowledge`、`settings` 和旧 RAG 模块增加理解成本，其中旧 dense retriever 还包含 pickle 读取。它们当前不可通过在线路由触达，但长期应迁入 `legacy/` 或删除，并单独执行一次依赖引用验证，避免未来误注册。

### R-08：缺少黄金题集与持续评测（中）

现有测试主要验证程序契约，不验证各年级、各科、各种手写和图形题的教育正确率。下一质量阶段应建立脱敏黄金题集，至少统计 OCR 题目召回率、答案识别准确率、判分一致率、知识点命中率、RAG 页码命中率和申诉推翻率。

### R-09：当前 API 密钥曾通过对话明文提供（高，需要用户在供应商侧处理）

本地 `.env` 已使用 0600 权限并被 `.gitignore` 忽略，代码和报告也没有复制密钥；但密钥此前已经出现在对话内容中。文件权限不能撤销这次暴露。应在对应模型供应商控制台立即吊销并重新生成 LLM 与 Embedding 密钥，再只把新值写入本机 `.env`。这项外部凭据轮换无法由项目代码代替。

---

## 八、建议的下一阶段验收门槛

正式给学生长期使用前，建议达到以下可量化门槛：

1. 每个支持科目至少 30 道脱敏真实照片题，覆盖印刷、手写、公式、图表、旋转、阴影和多页；
2. 题目召回率不低于 98%，不允许静默漏页；
3. 客观题判分准确率不低于 99%，主观题与教师一致率单独统计；
4. RAG 教材/年级/学期串库为 0，相关页 top-4 命中率有独立指标；
5. 所有模型版本、提示词版本、教材 SHA-256 与批改记录建立可追溯关联；
6. 完成 CDN 本地化、数据导出/删除、定时备份与恢复演练；
7. 若取消“个人 Mac”限制，先完成真正的身份认证和数据库迁移，再开放局域网或公网。

当前版本在既定的“个人 Mac、本机访问、姓名无密码”范围内，安全性、失败可见性、批改结果对齐和学生画像一致性已显著提高；但对 AI 教育产品而言，真正决定质量上限的下一步不是再增加 Agent 数量，而是建立真实黄金题集、教师复核标准和持续回归评测。
