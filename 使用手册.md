# StudyAgent AI 详细使用手册

> 适用版本：本机单用户服务版 v1.0  
> 最后核对日期：2026 年 9 月 3 日  
> 浏览器唯一入口：`http://localhost:8001/ui/`

本文既是学生和家长的操作教程，也是项目维护者的启动、教材索引、备份和故障排查手册。第一次使用建议依次阅读“快速开始”“登录规则”“作业批改”和“常见问题”；日常使用只需打开浏览器即可。

---

## 目录

1. [这套系统能做什么](#1-这套系统能做什么)
2. [开始前必须知道的规则](#2-开始前必须知道的规则)
3. [五分钟快速启动](#3-五分钟快速启动)
4. [首次登录与切换学生](#4-首次登录与切换学生)
5. [界面导航](#5-界面导航)
6. [主控台](#6-主控台)
7. [作业批改：完整操作流程](#7-作业批改完整操作流程)
8. [错题本与复习模式](#8-错题本与复习模式)
9. [题目询问：文字、图片和-pdf](#9-题目询问文字图片和-pdf)
10. [知识库与教材 RAG](#10-知识库与教材-rag)
11. [历史记录](#11-历史记录)
12. [“我的”：档案、趋势和偏好](#12-我的档案趋势和偏好)
13. [长期记忆如何工作](#13-长期记忆如何工作)
14. [数据存储、隐私与备份](#14-数据存储隐私与备份)
15. [教材维护与重新向量化](#15-教材维护与重新向量化)
16. [停止、重启和更新依赖](#16-停止重启和更新依赖)
17. [常见问题与排查](#17-常见问题与排查)
18. [当前教材范围](#18-当前教材范围)
19. [使用前验收清单](#19-使用前验收清单)

---

## 1. 这套系统能做什么

StudyAgent AI 是运行在 Mac 本机上的一年级至九年级 AI 学习助手，页面标题显示为“StudyAgent AI · 学习中枢”。它围绕一个学生档案完成以下闭环：

1. 学生用“姓名 + 年级 + 学期”进入自己的学习空间。
2. 学生先选科目，再上传手机拍摄的作业或试卷图片。
3. GPT-5.5 视觉模型直接查看原图中的印刷文字、手写答案、公式、图形、图表和圈画。
4. 系统只在当前学生的“年级 + 学期 + 科目”教材向量中检索相关片段。
5. AI 结合原图、教材片段和既往学情逐题批改。
6. 错题自动进入错题本，成绩和薄弱知识点进入学生画像。
7. 学生可以继续询问题目、复习错题，并在后续会话中获得更个性化的讲解。

本项目不支持高中内容，也不允许学生在界面中切换其他解析模型。作业批改、图片答疑和错题解析统一使用系统配置的 GPT-5.5。

---

## 2. 开始前必须知道的规则

### 2.1 登录没有注册页，也没有密码

- 第一次输入一个新姓名并登录，系统会自动创建档案。
- 以后输入相同姓名，会进入同一个档案。
- 姓名在同一台电脑上必须唯一，创建后不能修改。
- 系统没有密码保护。知道姓名的人可以进入该姓名对应的档案，因此只适合可信任的本机环境。
- 每次登录都要选择当前年级和学期；用同名再次登录时，新选择会更新原档案的年级和学期，不会创建第二个学生。

### 2.2 只使用 `localhost`

浏览器固定打开：

```text
http://localhost:8001/ui/
```

不要把 VS Code 内嵌网页、旧书签里的 `127.0.0.1` 和 `localhost` 混着用。它们都指向本机，但浏览器会把它们视为不同网站，Cookie 和缓存互不相同。当前项目已经把 `127.0.0.1:8001` 自动重定向到 `localhost:8001`，从而只保留一套登录状态。

### 2.3 本项目不开放局域网

StudyAgent AI 后端只监听 `127.0.0.1`。同一 Wi-Fi 下的手机、平板或其他电脑不能直接访问，这是当前版本的隐私和安全边界。手机拍完作业后，需要通过隔空投送、照片同步或数据线把图片交给这台 Mac，再从网页上传。

---

## 3. 五分钟快速启动

以下命令均在 macOS“终端”中执行。

### 3.1 进入项目目录

```bash
cd /Users/szl/Desktop/Project/StudyAgent-AI
```

### 3.2 确认 Conda 环境

本项目固定使用：

```text
/opt/miniconda3/envs/studybuddy
```

检查 Python：

```bash
/opt/miniconda3/envs/studybuddy/bin/python --version
```

如果环境尚不存在，创建并安装依赖：

```bash
/opt/miniconda3/bin/conda create -y -p /opt/miniconda3/envs/studybuddy python=3.13
/opt/miniconda3/envs/studybuddy/bin/python -m pip install -r requirements.txt
```

本项目不使用 `.venv`。

### 3.3 启动两个 Docker 数据服务

先确保 Docker Desktop 已启动，然后运行：

```bash
docker compose up -d qdrant everos
docker compose ps
```

正常情况下应看到：

- `studybuddy-qdrant-1`：`healthy`；保存教材向量。
- `studybuddy-everos-1`：`healthy`；保存长期学习记忆。

可以进一步检查：

```bash
curl http://127.0.0.1:6333/healthz
curl http://127.0.0.1:1995/health
```

### 3.4 启动网页和后端

```bash
./run.sh
```

看到 Uvicorn 启动日志后不要关闭该终端。`run.sh` 只是快捷入口，它会明确使用 `/opt/miniconda3/envs/studybuddy/bin/python`，避免误用系统 Python 或 `.venv`。

### 3.5 打开网页

在 Chrome、Safari 或 Edge 中访问：

```text
http://localhost:8001/ui/
```

不要双击 `web/index.html`，也不要用 VS Code 的静态预览服务器打开。那样页面虽然可能显示出来，却无法正确调用登录、批改、知识库和长期记忆接口。

---

## 4. 首次登录与切换学生

### 4.1 第一次登录

1. 在“姓名”中输入学生真实姓名或约定的唯一称呼。
2. 选择当前年级，只能选择一年级至九年级。
3. 选择“上学期（上册）”或“下学期（下册）”。
4. 点击“进入我的学习空间”。

第一次登录会自动完成建档，不需要单独注册。

### 4.2 再次登录同一学生

输入完全相同的姓名即可进入原档案。系统会规范化姓名两端的空格和连续空格，因此误输入姓名末尾空格通常不会产生新用户。

登录页本次选择的年级和学期会成为该学生的新年级和学期。例如，同一学生原来是七年级下册，下一学年用相同姓名选择八年级上册登录后：

- 姓名和用户 ID 不变；
- 原有错题、历史和长期记忆仍属于该学生；
- 当前科目列表切换为八年级科目；
- 知识库切换为八年级上册教材；
- 新作业只检索八年级上册所选科目的教材。

### 4.3 切换到另一个学生

1. 点击左下角“退出”，或进入“我的”点击“退出当前档案”。
2. 回到登录页后输入另一个唯一姓名。
3. 选择该学生自己的年级和学期并登录。

不同姓名对应不同用户 ID。错题、作业原图、问答会话、偏好、学生画像和 EverOS 记忆均分开保存。

---

## 5. 界面导航

电脑端左侧有七个栏目：

| 栏目 | 用途 |
|---|---|
| 主控台 | 查看学习总览、正确率、薄弱点和今日建议 |
| 作业批改 | 选择科目并上传作业或试卷图片 |
| 错题本 | 查看、筛选、解析、复习和管理错题 |
| 题目询问 | 用文字、图片或 PDF 向 AI 提问 |
| 知识库 | 查看当前年级、学期的教材及索引状态 |
| 历史记录 | 回看作业批改记录和答疑会话 |
| 我的 | 修改年级/学期、设置偏好、查看长期学情 |

页面左上角还有“← 后退”和“→ 前进”：

- 它们用于返回刚才访问过的 StudyAgent AI 栏目。
- 它们不会修改或删除数据。
- 到达历史最前或最后时，对应按钮会自动变为不可点击。
- 这组按钮和浏览器自身的前进、后退按钮不是一回事。

左下角状态区每 15 秒检查一次 API，显示“检测中”“在线”或“离线”，并显示错题数量和掌握率。如果整个网页已经打开却仍操作失败，可按第 17 节继续做健康检查。

---

## 6. 主控台

主控台用于快速判断“今天最应该学什么”。

### 6.1 顶部学生卡片

这里显示姓名、当前年级/学期、连续学习天数和个性化问候。点击姓名旁的编辑图标会进入“我的”。

### 6.2 科目正确率分析

- 有批改记录时：按近期成绩显示各科平均正确率，较弱科目优先排列。
- 只有错题、暂无完整成绩数据时：显示各科错题分布。
- 完全没有记录时：显示引导提示。

### 6.3 最需攻克的知识点

系统从历史批改中累计薄弱知识点。点击某个知识点会进入错题本并自动筛选相关题目。

### 6.4 近期学习记录与今日建议

近期记录显示科目、作业/考试类型、日期和正确率。今日建议会优先提示未掌握错题、正确率偏低的科目或尚未开始的首次作业。

---

## 7. 作业批改：完整操作流程

### 7.1 第一步：选择科目

进入“作业批改”后，系统只显示当前年级实际开设的科目。例如九年级显示语文、数学、英语、物理、历史、化学，不会显示生物和地理。

务必先选对科目。科目决定：

- 检索哪一本教材；
- 错题进入错题本后的归属；
- 学情统计归入哪一科；
- 后续错题解析使用哪一本教材。

### 7.2 第二步：选择“作业”或“考试”

- 普通练习、课后题、练习册选择“作业”。
- 月考、期中、期末等选择“考试”，并建议填写考试名称。

这会影响“我的”页面中的作业/考试趋势分类。考试名称可以不填，但填写后更容易在近期记录中识别。

### 7.3 第三步：准备清晰照片

支持的格式：JPG、JPEG、PNG、WEBP、GIF、HEIC、HEIF。单张不超过 20 MB，可以一次上传多张。

iPhone 常见的 HEIC/HEIF 会在本机自动转成高质量 JPEG，再传给视觉模型。原始照片仍按当前用户保存，不会作为公共静态文件开放。

为了让 AI 同时看清文字和图形，建议：

1. 镜头尽量与纸面平行，不要斜拍。
2. 一张图包含完整题干、图形和学生答案，不要把关键条件裁掉。
3. 避免阴影、反光、手指遮挡和严重折痕。
4. 手写字要有足够对比度，铅笔字过浅时先提高现场光线。
5. 多页可以按任意顺序选择。系统会读取图片中实际印刷的“大题号 + 小问号”后重新排序，不读取文件名决定顺序；仍建议拍到题号并避免把题号裁掉。
6. 正反面作业分别拍摄，不要把两页缩成一张拼图。
7. 图表、几何图、连线题和圈画题不要先只做传统 OCR；直接上传完整原图才能保留位置关系。

### 7.4 第四步：上传并开始批改

点击上传区域选择图片，也可以从 Finder 拖入。预览出现后：

- 点击图片右上角“×”可移除该页；
- 点击“继续添加”可补充下一页；
- 点击“← 更换科目”会清空已选图片，避免图片误归科目；
- 确认后点击“开始批改”。

批改需要调用远程 GPT-5.5 和本地教材向量库，耗时会随图片数量、题目数量和网络状况变化。处理中显示的是服务端当前真实阶段和真实进度，不是定时轮播演示。阶段包括接收与校验、视觉识别、按题号整理、教材检索、逐题判分、知识点标注、整卷归纳和保存。处理中请不要重复点击、刷新页面或关闭服务终端。

### 7.5 AI 实际如何批改

界面中的“OCR”是一个结构化识别阶段，但核心由视觉模型直接看原图完成，并非只把图片机械转成文字。流程如下：

1. 视觉识别完整大题题干、实际印刷的大题号与小问号、学生答案、公式、图形、表格和圈画位置。
2. 根据图片内容中的题号整理多页题目顺序并保持“大题—小问”归属；文件名不参与排序。
3. 用识别出的题目去当前教材的 Qdrant 向量中检索。
4. GPT-5.5 同时参考原图、识别结果、教材片段和学生历史薄弱点。
5. 逐个小问给出正确、错误、部分正确或未作答判断，并为所有结果生成答案对照和解题思路。
6. 标注知识点和难度；错误原因由 AI 针对实际作答自由概括，不强制塞入固定分类。
7. 保存批改历史，错题自动进入当前学生的错题本。
8. 先把本次成绩和薄弱点可靠写入本地学生画像，再尝试同步到 EverOS 长期记忆；EverOS 或其模型通道暂时不可用时不影响本地档案。

### 7.6 阅读批改结果

结果顶部显示总分或正确率，以及正确、错误和总题数。每道题可显示：

- 题型和难度；
- 得分；
- 是否命中历史薄弱点；
- 完整大题题干，以及归属于该大题的每个小问；
- 学生答案与正确答案对照（答对的题也显示）；
- 每个小问的解题思路（答对、答错和部分正确均显示）；
- AI 结合实际作答自由判断的具体错误原因；
- 简短讲评。

结果区采用可伸缩的批改工作台。桌面端可用“收起历史 / 展开历史”动态释放横向空间；每道大题按左右两栏展示：

- 左侧上方展示共享大题题干和全部小问，错误或部分正确的小问使用红色提示，正确小问使用绿色提示；
- 左侧下方展示从学生上传原图中截取的完整大题区域，包含题干、图形和相关小问；点击图片可以放大；
- 右侧依次展示学生实际书写的步骤与答案、错误诊断与教材复习位置、正确答案、完整解题思路；
- 长解答会按步骤和自然段自动分隔；规范的行内、独立数学公式使用 KaTeX 排版，过宽公式可以横向滚动，不会挤压整个页面；
- 教材章节与页码只会从本次实际检索到的教材片段中提取，系统不会猜测页码；
- 无法可靠判断裁剪区域时会显示完整上传页，避免因为裁剪过窄而漏掉图形或题干。

旧批改记录可能没有保存结构化共享题干或精确裁剪坐标。系统会尽量关联当时的原始上传页并明确提示；重新上传后会按新结构完整保存。

“部分正确”会和错题一起进入错题本，便于后续复习。

### 7.7 AI 判错时怎么办

- 点击“重新 AI 审题”：AI 会再次查看该题对应的原始页面并复核。若复核为正确，错题本和最近成绩会同步修正。
- 点击“我做对了”：直接人工覆盖为正确，适用于你已经确认 AI 判错的情况。

人工覆盖会改变错题和成绩统计，请只在核对题干、答案和评分要求后使用。AI 批改只能辅助学习，学校正式成绩仍以老师判定为准。

---

## 8. 错题本与复习模式

### 8.1 统计和筛选

顶部显示错题总数、已掌握、待攻克和掌握率。可以点击统计卡片，或按当前年级的科目按钮筛选。点击“待攻克”只显示尚未掌握的题。

批改与解析模型固定显示为“ChatGPT · gpt-5.5”，没有其他模型选项。

### 8.2 错题卡片

错题本只接收“错误”和“部分正确”的小问，不保存做对的题。错题按同一份作业、同一大题归组，采用与批改结果相同的左右工作台，并直接展示原题截图、学生步骤、错误原因、薄弱知识点、教材章节/页码、正确答案和解题思路，不需要每次点击后等待生成解析。主要操作：

- “标记已掌握”：在已掌握与未掌握之间切换。
- “删除”：永久删除该错题；此操作不可撤销。

新批改产生的错题会同步保存完整解析和实际命中的教材位置，因此打开错题本即可阅读。旧错题缺少历史字段时会明确显示“旧记录未保存”，不会伪装成完整解析。

### 8.3 复习模式

从薄弱知识点筛选条进入复习模式后：

1. 先独立思考题目。
2. 点击“翻开答案”。
3. 对照当时的错误答案、正确答案和错误类型。
4. 需要时点击“AI 详细解析”。
5. 已经会做则点击“我会了，下一题”；暂时不会可点击“跳过”。

“掌握”不等于删除。题目仍保留在档案中，可以通过“已掌握”统计再次找到。

---

## 9. 题目询问：文字、图片和 PDF

### 9.1 先选科目

顶部科目只显示当前年级实际开设的课程。先选科目再提问，系统才能检索正确教材。

### 9.2 纯文字提问

在输入框中写完整题目或具体疑问：

```text
为什么一元二次方程判别式小于 0 时没有实数根？请按九年级教材的方式讲解。
```

- Enter：发送。
- Shift + Enter：换行。
- 在同一会话中继续追问时，AI 会保留最近的对话上下文。

### 9.3 图片提问

点击回形针选择 JPG、PNG、WEBP、GIF、HEIC 或 HEIF 图片。可以同时补充一句要求，例如“只讲第 3 题”“请指出我的作图哪里错了”。

图片会直接进入视觉模型，因此适合：

- 含公式、手写步骤的题；
- 几何图、函数图像、实验装置图；
- 表格、地图、插图和圈画题；
- 传统 OCR 难以保留空间关系的内容。

### 9.4 PDF 提问

回形针也支持不超过 20 MB 的 PDF。当前处理规则是：

- 最多读取前 20 页；
- 最多把前 20,000 个字符交给问答模型；
- 适合本身带有可复制文字层的 PDF；
- 扫描版 PDF 如果没有文字层，会提示“请把题目页面截图后按图片上传”。

PDF 不是按图片逐页视觉分析。如果问题依赖图形、版面或手写标注，最可靠的方法仍是把相关页面截图并按图片上传。

### 9.5 会话管理

- 第一条消息发出后，会话自动保存。
- 点击“新会话”开始一个独立话题。
- 左侧会话栏可以恢复旧会话。
- “清空”只清空当前页面上的消息显示，不等同于删除服务器中已经保存的历史会话。
- “历史记录 → 答疑会话记录”可查看已保存的完整会话。

---

## 10. 知识库与教材 RAG

### 10.1 页面显示什么

知识库只显示当前登录档案的年级和学期教材。每本教材包含：

- 学科和 PDF 文件名；
- PDF 页数；
- 向量知识片段数量；
- “已索引”“待索引”或“文件缺失”状态；
- “打开”按钮；
- 尚未索引时的“索引”按钮。

学生不能上传、重命名或删除系统教材。

### 10.2 “已索引”是什么意思

系统先从 PDF 每页提取文字；扫描页会使用提前生成的 macOS Vision OCR 结果。之后把文本按约 700 字、100 字重叠切分，用 `qwen3.7-text-embedding` 生成 1024 维向量，并写入本机 Docker Qdrant。

向量数据库不是把 PDF 简单复制进去，而是保存“教材片段 + 页码 + 年级 + 学期 + 科目 + 文件版本哈希 + 向量”。这样问答时才能快速找到语义最接近的教材段落。

### 10.3 如何保证不会检索错教材

每本教材有唯一 ID，例如：

```text
grade_07_lower_math
grade_09_lower_chemistry
```

每次检索都同时使用当前用户的年级、学期和所选科目生成教材 ID，并在 Qdrant 中按该 ID 和 PDF 文件哈希过滤。因此七年级下册数学不会混入九年级下册数学。

点击“打开”时，URL 同时携带教材 ID 和完整文件名；后端会再次和当前登录档案核对。若年级刚刚发生变化，旧链接会返回冲突提示，而不是静默打开另一册书。所有教材共用一个专用 PDF 标签页，以免屏幕上同时留着旧年级教材造成误判。

### 10.4 RAG 的作用边界

RAG 是“检索增强生成”：先找到相关教材片段，再让 GPT-5.5 结合题目进行讲解。它能提高教材一致性，但不保证每次都能找到完全对应的段落。

- 显示“教材 RAG”：本次确实检索到了片段。
- 没有该标记：AI 可能只依据题目和通用能力回答，应更谨慎核对。
- 教材原文、老师评分标准与 AI 结论冲突时，以教材和老师为准。

---

## 11. 历史记录

历史记录分成两个标签页。

### 11.1 作业批改记录

列表按时间显示科目、题数、正确/错误数、正确率和薄弱知识点。点击一条记录可查看：

- 批改时间；
- 正确、错误、部分正确、未答统计；
- 试卷标签和薄弱点；
- 每道题的判断和简评。

这里用于回看当时结果，不会重新调用 AI。

### 11.2 答疑会话记录

列表显示科目、更新时间、预览和问答轮数。点击后可查看用户和 AI 的完整消息以及会话摘要。

---

## 12. “我的”：档案、趋势和偏好

### 12.1 修改年级和学期

“我的 → 个人信息”中可以修改年级和学期，选择后会自动保存。保存成功后：

- 左侧和顶部显示的新年级立即生效；
- 作业与答疑科目重新匹配；
- 知识库切换到对应教材；
- 当前页面缓存的上一次批改结果会清除，避免和新年级混淆。

姓名输入框是禁用状态，不能修改。

### 12.2 性别和 MBTI

性别与 MBTI 为可选的本机自我描述。不知道 MBTI 时保持“不知道 / 暂不填”即可。它们不会进入 Agent 判分、教材 RAG 或新写入的 EverOS 长期记忆，也不会被当作学习能力证据。

### 12.3 解释偏好

- 详细程度：简短、适中、详细。
- 讲解风格：生动形象、简洁客观。

偏好保存后会进入当前学生的本地画像，并用于后续讲解。它不会改变标准答案或评分规则。

### 12.4 学情区域

“我的”还会显示：

- 当前、历史最长和累计学习天数；
- 作业/考试/全部成绩趋势；
- 各科平均正确率；
- 薄弱知识点；
- 近期成绩记录；
- 当前教材数量和索引数量。

没有批改数据时显示空状态是正常现象。

---

## 13. 长期记忆如何工作

StudyAgent AI 有两层互补记忆。

### 13.1 本地结构化档案

每个用户目录中保存可直接统计的内容：成绩、错误类型、薄弱知识点、连续学习、偏好、问答会话和作业历史。主控台和“我的”主要读取这一层。

### 13.2 EverOS（EverMemOS）长期记忆

EverOS 运行在 Docker 中。系统会把作业摘要、错题、薄弱点、会话摘要和讲解偏好写入当前用户自己的记忆空间。之后答疑和错题讲解前，会用当前问题检索相关历史，形成更连续的学生画像。正式判分不读取 EverOS，避免过时或已撤销的自然语言记忆影响客观评分。

EverOS 暂时不可用时，登录和基础页面仍能使用；长期记忆写入或检索会降级。本地结构化档案不会因此自动消失。

### 13.3 什么会写入长期记忆

- 作业或考试的科目、题数、正确率和错误类型；
- 错题题干、学生答案、正确答案和知识点；
- 多次出现的薄弱知识点；
- 问答会话摘要；
- 年级、学期和解释偏好。性别与 MBTI 只保存在本机界面档案中，不写入新 EverOS 记忆。

不要在题目和姓名中输入不必要的敏感信息。

---

## 14. 数据存储、隐私与备份

### 14.1 数据分别存在哪里

| 数据 | 位置 |
|---|---|
| 用户、登录会话、教材索引状态 | `data/studybuddy.db` |
| 分用户档案、错题、历史、作业图片、会话图片 | `data/users/<用户 UUID>/` |
| 教材 PDF | `TextBook/<年级>/` |
| 教材 OCR 文字 | `data/textbook_ocr/` |
| 教材索引清单 | `data/textbook_indexes/` |
| Qdrant 教材向量 | Docker 卷 `studybuddy_qdrant_data` |
| EverOS 长期记忆 | Docker 卷 `studybuddy_everos_data` |
| 后端日志 | `data/logs/` 和 `data/user/logs/` |

`.env` 包含真实 API 密钥，已经被 `.gitignore` 排除。不要截图、上传、提交或转发该文件。若密钥曾经公开，应到对应平台立即撤销并重新生成。

### 14.2 备份项目内数据

先停止 StudyAgent AI 后端写入，再在项目根目录执行：

```bash
mkdir -p backups
tar czf "backups/studybuddy-files-$(date +%Y%m%d-%H%M%S).tar.gz" \
  data TextBook .env
```

备份包含 API 密钥和学生数据，应存放在受保护的位置。

### 14.3 备份 Qdrant 和 EverOS Docker 卷

```bash
docker run --rm -v studybuddy_qdrant_data:/data:ro -v "$PWD/backups":/backup \
  alpine tar czf /backup/qdrant-data.tar.gz -C /data .

docker run --rm -v studybuddy_everos_data:/data:ro -v "$PWD/backups":/backup \
  alpine tar czf /backup/everos-data.tar.gz -C /data .
```

恢复前必须停止对应容器并确认目标卷名称。不要执行 `docker compose down -v`，该命令会删除 Qdrant 和 EverOS 的持久化数据卷。

---

## 15. 教材维护与重新向量化

### 15.1 文件夹和文件名规则

教材放在年级文件夹中：

```text
TextBook/
├── 一年级/
├── 二年级/
├── …
└── 九年级/
```

文件名必须严格为：

```text
学科_年级_上册.pdf
学科_年级_下册.pdf
```

例如：`生物_八年级_下册.pdf`。多余空格、不同下划线、错误年级或“上学期”字样都会导致系统判断文件缺失。

### 15.2 什么时候必须重新索引

- 新增了原来缺失的教材；
- 替换或修订了 PDF；
- 更换 embedding 模型或向量维度；
- 知识库显示“待索引”或索引版本过期；
- Qdrant 数据卷丢失或重建。

### 15.3 处理扫描版教材

先执行 macOS Apple Vision OCR：

```bash
/opt/miniconda3/envs/studybuddy/bin/python scripts/ocr_textbooks.py
```

脚本支持断点续跑，已有 OCR 结果会保留。

### 15.4 索引全部教材

```bash
/opt/miniconda3/envs/studybuddy/bin/python scripts/index_textbooks.py
```

已完成且 PDF 哈希、模型和索引版本均一致的教材会自动跳过。

只索引一本教材，例如九年级下册化学：

```bash
/opt/miniconda3/envs/studybuddy/bin/python scripts/index_textbooks.py \
  --grade 9 --semester lower --subject chemistry
```

强制重建：

```bash
/opt/miniconda3/envs/studybuddy/bin/python scripts/index_textbooks.py \
  --grade 9 --semester lower --subject chemistry --force
```

索引过程会调用千问 embedding API，批量重建 70 本教材会消耗较长时间和接口额度。不要在 Qdrant 未启动或 embedding 密钥无效时反复执行。

### 15.5 在页面中索引

知识库某本书显示“待索引”时，可以点击该书的“索引”。这是后台任务；提示“已启动”不等于已经完成。稍后点击“刷新”，直到状态变成“已索引”。批量维护仍建议使用命令行脚本，因为命令行会逐本显示成功或失败。

---

## 16. 停止、重启和更新依赖

### 16.1 停止网页服务

在运行 `./run.sh` 的终端按 `Control + C`。这只停止 StudyAgent AI 后端，不会删除学生数据，也不会停止 Docker 数据服务。

### 16.2 停止 Docker 服务但保留数据

```bash
docker compose stop qdrant everos
```

下次恢复：

```bash
docker compose up -d qdrant everos
```

### 16.3 代码或依赖更新后重启

```bash
cd /Users/szl/Desktop/Project/StudyAgent-AI
/opt/miniconda3/envs/studybuddy/bin/python -m pip install -r requirements.txt
docker compose up -d qdrant everos
./run.sh
```

开发模式会监视 Python 源码并自动重载；修改依赖或 `.env` 后仍应完整停止并重新启动。

---

## 17. 常见问题与排查

### 17.1 浏览器提示“无法访问此网站”或“连接被拒绝”

原因通常是 StudyAgent AI 后端没有运行。检查：

```bash
curl http://localhost:8001/api/v1/health
```

正常结果：

```json
{"status":"ok"}
```

若连接失败，在项目目录重新执行 `./run.sh`，并查看终端中的报错。端口被占用时：

```bash
lsof -nP -iTCP:8001 -sTCP:LISTEN
```

不要同时启动两个 StudyAgent AI 进程。

### 17.2 登录页提示“无法连接本机服务”或“Failed to fetch”

按顺序检查：

1. 地址必须是 `http://localhost:8001/ui/`。
2. 不要通过 `file://`、VS Code Live Server 或其他端口打开 `index.html`。
3. 运行上一节的健康检查。
4. 确认页面不是旧缓存：按 `Command + Shift + R` 强制刷新。
5. 仍失败时关闭该网页标签，重新输入标准地址。

当前后端会禁止动态页面缓存，并清理旧版 Service Worker；强制刷新通常只需做一次。

### 17.3 VS Code 内网页和普通浏览器显示不一样

VS Code 内嵌 WebView 和普通浏览器是两个独立浏览环境，各自有 Cookie、缓存和 Service Worker。以普通浏览器打开的 `http://localhost:8001/ui/` 为准，不建议把 VS Code 预览作为正式入口。

### 17.4 年级显示正确，但打开的是旧教材

1. 关闭以前手动打开的旧 PDF 标签页。
2. 回到“知识库”点击“刷新”。
3. 再点击目标教材“打开”。
4. 若提示年级或学期已变化，刷新 StudyAgent AI 页面后重试。

当前版本会校验教材 ID、完整文件名和登录档案，并复用同一个教材标签页；正常情况下不会再静默打开旧年级教材。

### 17.5 知识库显示“待索引”或“文件缺失”

- 文件缺失：核对 `TextBook/<年级>/` 中的文件名和课程范围。
- 待索引：确认 Qdrant 和 embedding API 正常，然后点击“索引”或运行 `scripts/index_textbooks.py`。
- 索引后仍不变：点击“刷新”，并查看 `data/logs/` 中最新日志。

### 17.6 上传 HEIC 后失败

当前版本已支持 HEIC/HEIF 并自动转 JPEG。若仍失败：

```bash
/opt/miniconda3/envs/studybuddy/bin/python -m pip install -r requirements.txt
```

然后重启服务。损坏或非标准 HEIC 文件可先在“预览”中导出为 JPEG 再上传。

### 17.7 PDF 提示没有文字层

这表示 PDF 是扫描图片，无法直接复制文字。打开 PDF，把相关题目页导出或截图为 PNG/JPG，再在“题目询问”中按图片上传，视觉模型才能查看图形和版面。

### 17.8 AI 没有显示“教材 RAG”

检查：

1. 提问前是否选对科目；
2. 知识库对应教材是否“已索引”；
3. Qdrant 是否 healthy；
4. 问题是否过短或缺少上下文。

可以把题干和“请按当前教材相关章节讲解”一起提交，提高检索针对性。

### 17.9 AI 看错字、漏题或误解图形

- 重拍更清晰、更正的图片；
- 保留完整题干和图形；
- 一页内容过密时分成两张近照；
- 在题目询问中补充“只看第几题”或指出模糊位置；
- 批改结果中使用“重新 AI 审题”。

视觉模型比纯 OCR 更适合图形，但仍可能出错。

### 17.10 EverOS 或 Qdrant 不健康

```bash
docker compose ps
docker compose logs --tail=100 everos
docker compose logs --tail=100 qdrant
```

尝试重启：

```bash
docker compose restart everos qdrant
```

不要用 `down -v` 解决普通故障，因为 `-v` 会删除持久化数据。

### 17.11 API 密钥或模型调用失败

只检查 `.env` 中是否存在且没有多余引号、空格或 Markdown 链接格式，不要把值打印到聊天或截图中。修改 `.env` 后重启 StudyAgent AI；EverOS 也使用其中的模型配置，因此必要时同时执行：

```bash
docker compose up -d --force-recreate everos
```

---

## 18. 当前教材范围

每个表格单元格中的科目都同时拥有上册和下册，共 70 本教材。

| 年级 | 课程 |
|---|---|
| 一年级 | 语文、数学 |
| 二年级 | 语文、数学 |
| 三年级 | 语文、数学、英语 |
| 四年级 | 语文、数学、英语 |
| 五年级 | 语文、数学、英语 |
| 六年级 | 语文、数学、英语 |
| 七年级 | 语文、数学、英语、生物、历史、地理 |
| 八年级 | 语文、数学、英语、物理、生物、历史、地理 |
| 九年级 | 语文、数学、英语、物理、历史、化学 |

特别注意：

- 英语从三年级开始。
- 物理为八、九年级。
- 生物和地理为七、八年级。
- 历史为七至九年级。
- 化学只有九年级。
- 不包含任何高中教材或高中科目配置。

---

## 19. 使用前验收清单

维护者在正式交给学生使用前，可以逐项确认：

- [ ] `docker compose ps` 中 Qdrant 和 EverOS 都是 healthy。
- [ ] `curl http://localhost:8001/api/v1/health` 返回 `{"status":"ok"}`。
- [ ] 浏览器只使用 `http://localhost:8001/ui/`。
- [ ] 登录页可用新姓名首次登录，也可用相同姓名回到原档案。
- [ ] “我的”修改年级/学期后，科目按钮和知识库同步变化。
- [ ] 当前年级知识库中的每本教材都显示正确文件名和“已索引”。
- [ ] 点击“打开”看到的 PDF 封面年级、学期与文件名一致。
- [ ] 作业上传可以选择 JPG/PNG 和 iPhone HEIC 图片。
- [ ] 题目询问支持纯文字、图片和带文字层的 PDF。
- [ ] 错题解析模型只显示 GPT-5.5。
- [ ] 批改后，错题本、历史记录和“我的”成绩趋势同步更新。
- [ ] 切换到另一个姓名后，看不到前一个学生的错题和历史。
- [ ] 已完成 `data/`、`.env`、Qdrant 卷和 EverOS 卷的安全备份。

如果以上项目全部通过，系统即可进入日常使用。
